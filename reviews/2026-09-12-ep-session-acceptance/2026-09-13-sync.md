# 2026-09-13 deployment and main synchronization

This synchronization includes the EP session guard, read-only session acceptance
module/CLI/tests, deployment helpers and review documentation. Earlier README
statements about not committing describe those earlier execution rounds.

## Deployment verification

The five runtime/test files below were compared byte-for-byte using SHA-256 against
the deployed SG locations before this commit. All matched the local working tree.

| File | SG location |
| --- | --- |
| `scripts/run_ep_event_worker.py` | `releases/ep-consumers-0329a78620e7-sessionguard` |
| `tests/test_ep_worker_market_guard.py` | Same release |
| `scripts/audit_ep_session.py` | `tools/ep-session-audit-20260912-v2` |
| `src/breakouts/ep/session_audit.py` | Same standalone tool |
| `tests/test_ep_session_audit.py` | Same standalone tool |

All paths are under `/home/projects/quant`. The independent tool reads production
databases; it is not a replacement strategy release. Historical release names still
refer to their original baseline plus reviewed overlay, not the new main commit.

The price/news/source and EP review timers are enabled and waiting for September 14.
The hourly momentum timer remains disabled. No configuration, model budget,
notification route or service schedule was changed during this synchronization.

The relevant offline suite was rerun in the deployed SG tool directory:
**93 passed in 29.70 seconds**. Receipt: `/home/projects/quant/tmp/ep-pre-main-20260913.xml`.

SG's root checkout still contains parallel cup-handle and data-repair work. It was
not reset, pulled over, staged or committed by this task. Deployment verification
therefore applies to the actual EP release/tool paths, not a claim that the entire
SG root checkout is clean or identical to main.

## Artifact boundary

The JSON/XML receipts, price/queue reports and screenshot reference JSON remain in
the existing local and SG review directories under the repository's ignore rules.
They are not included in this source commit. No database, key, private config or
Discord webhook is included. The documented report locations refer to local/SG
artifacts, not files guaranteed to exist in a fresh Git clone.

Real production-session acceptance remains pending the September 14 session;
deployment checks and offline regression are not evidence of completed live recall.
