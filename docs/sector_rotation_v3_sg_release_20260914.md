# 板块轮动 V3：SG 正式发布记录（2026-09-14）

状态：**SG 已完成本次发布验收，恢复正常定时调度。** 本记录不宣称策略预测有效或多日调度已经验证。

## 发布范围与代码

- PR #7 的基线：`1f4c5bd`。
- 生产适配修复：`10649ba`；独立参考刷新时序补齐：`fa79230`。
- 两项修复均已快进合并并推送远端 main；本地 main 同步，无强制推送。
- SG 使用经过清单核对的增量文件安装，不用 `git reset/pull` 覆盖服务器上的独立模块。服务器旧 `.git` 或 `.deploy-commit` 不作为实际代码版本证明。
- 功能发布安装 72 个差异文件，对 726 个文件核对 Git blob 哈希；追加本发布记录后清单为 73 / 727。未在范围内的文件保持发布前内容。EP 独立部署保留；不改动多因子、模拟盘、回测、茶杯柄和动量核心算法。

## 备份与保护

SG 备份目录：`/home/projects/quant-backups/sector-rotation-v3-20260914-10649ba`。

包括发布前源码、systemd 配置、轮动 latest/last_attempt、存在时的市值 latest 和盘前 outbox SQLite 一致性备份。代码暂存和部署/回滚脚本在同名 `quant-staging` 目录下的 `deployment-ops/`。

切换期间暂停盘前及轮动相关定时器和运维 watchdog，使用运行时维护条件阻止意外触发；重启主站和运维服务。没有停止或覆盖 EP 独立服务。回滚仅还原变更路径、发布指针和被本次更改且未尝试发送的板块摘要，不整库覆盖动量发送历史。

## 已完成验证

- 最终代码针对性回归：**305 tests passed，44 subtests passed**。包含轮动、持仓、热力图、关联、盘前、systemd、领域隔离、模拟盘完整性、茶杯柄和运维。两个警告为既有测试依赖弃用提示。
- SG 主站 `/`、`/research`、`/paper`、`/breakouts`、`/group-analytics`、`/group-analytics/daily`、轮动 API 均响应 200；运维页及 healthz 响应 200；未认证首页响应 401。
- 正式持仓采集：17/17 成功。去重成员 636 只。BOTZ 等非美/未解析仓位仍计为覆盖缺口，不伪装全基金广度。
- 独立参考刷新正式 unit 成功。主站热力图实际 API 返回 available、11 个板块；5,736 个股票/ADR 中 5,589 个有可映射正市值（97.4372%），147 个缺市值。
- 热力图 1/5/20 日分别可计算 5,575/5,565/5,502 个，缺价格 14/24/87 个；每个窗口可计算、缺市值、缺价格加总均为 5,736。
- 当前完整交易日应为 2026-09-11；部署时美国仍在周末，不能要求未发生的 9 月 14 日收盘数据。

## 调度

以下为美国东部时间，systemd 自动处理夏令时：

| 任务 | 时间 | 说明 |
|---|---|---|
| 主题价格与成员行情 | 交易周工作日 17:30 | 独立价格快照，之后尝试限时参考刷新 |
| ETF 持仓观测 | 周一 12:00 | 当前观测，不宣称历史 PIT |
| 市值参考刷新 | 工作日 05:45 | 在 SG 11:30 宽基调度之后重新绑定当前版本 |
| 个股关联重试 | 工作日 07:00 | 400/550 MiB，15 分钟，成功回调仅板块 prepare |
| 双频道准备 | 工作日 07:00 | 不依赖关联任务或新参考任务 |
| 板块摘要换版 | 工作日 09:00 | 只替换可替换的未发送摘要 |
| 正式发送 | 工作日 09:20 | 保留 XNYS、日期、新鲜度、幂等保护 |

原板块关联任务保留 SG 周二至周六 13:15 调度。

## 最终验收

- 正式价格任务于 SG 00:15 启动，含 636 只成员刷新，约 23 分 37 秒完成；systemd 记录内存峰值 198.6 MiB。价格 run：`rot_20260911_a62570c265e63a88`。
- 正式关联 retry unit 于 SG 00:38:55 启动，约 45 秒完成，systemd 记录峰值 283.8 MiB，低于未调整的 400/550 MiB 限制，无重启。最终 run：`rot_20260911_5d71f854920e2907`。
- 最终 schema 为 `rotation.v3.0`，22/22 主题，source_session 为 2026-09-11，freshness=current；冻结重放 MATCH。最新页 last_attempt 和 price/linkage checks 全为 SUCCESS。
- 17 个 ETF 的广度状态仍为 `OBSERVATION_ONLY_NO_PROVIDER_DATE`：当前持仓可观察，但供应商生效日未知，不能升格为历史 PIT 或正式广度确认。另 5 个自定义篮子按自身成员口径显示。
- 正式 root `OnSuccess=quant-premarket-prepare-sector-rotation.service` 已真实触发，结果从 PREPARED 变为 PREPARED_REPLACED。价格版摘要哈希 `383d80e7…dd7ddae` 替换为关联版 `16308590…f6edd57`，最终 PENDING 对应最终 run，attempts=0。
- 当时纽约为周日，为验收未发送摘要而临时给 prepare 加 `--session 2026-09-14`，没有修改系统时间或使用 `--send`。该运行时覆盖已删除，生产 unit 恢复正常日历取日逻辑。只验证了回调和换版机制，不把这次指定交易日验收说成自然盘前时段已经运行。
- 与部署前数据库逐行比较：动量记录未改变，SENT/SENDING/UNKNOWN 记录未改变，没有实际 Discord 请求。板块 9 月 14 日待发送摘要已准备并与最终 run 匹配。
- 五个新增 timer 已启用；原板块关联、双频道准备、发送和运维 watchdog 调度已恢复。当前下次参考刷新为北京时间 9 月 14 日 17:45，关联/准备 19:00，板块换版 21:00，发送约 21:20（timer 含秒级抖动）。
- 最后页面/热力图/队列验收再次通过；主站与运维服务 active，NRestarts=0；维护标记、部署 guards 和临时交易日覆盖均为空。

仍需后续正常交易日观察：全量动量 prepare 与独立关联的实际并行资源情况、连续交易日成员数据/覆盖更新、真正的定时 Discord 到达情况。新参考任务可从 systemd 和热力图缺口查看状态，本次未新增专门的运维适配器。未做 TradingView 数值对账，也未验证策略预测性。

本次不额外发送 Discord 验收消息。正常定时发送仍按既有授权运行。运行验收不等于 TradingView 数值对账、策略预测性或多日并发稳定性验证；这些边界不会因上线成功而改变。
