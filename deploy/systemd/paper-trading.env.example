# Install as /etc/quant/paper-trading.env with mode 0640 and owner root:quant.
# The paper worker needs market data only. It has no broker credentials and
# cannot place live orders.
FMP_API_KEY=replace_with_your_fmp_key
